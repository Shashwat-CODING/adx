# adx
The developer-first CLI that makes Android development painless. Replaces messy ./gradlew and adb commands with instant builds, smart multi-device running, automated Maven dependency management, logcat filtering, and zero-headache cache nuking.
